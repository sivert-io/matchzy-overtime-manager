import { Logger as print } from "lovely-logs";

export function endMatch(winner: "ct" | "t") {
  print.info("Triggering match force end (this logic can be expanded)");
  // Implement logic to force end match here
}
